# 📘 Инструкция по настройке WHMCS интеграции для VPS заказов

## 🎯 Обзор

Страница `/pages/vds/virtual.php` теперь интегрирована с вашим WHMCS биллингом на `https://bill.sthost.pro`.
Кнопки "Замовити зараз" автоматически добавляют VPS в корзину WHMCS.

---

## ⚙️ Настройка WHMCS Product IDs

### Шаг 1: Создайте продукты в WHMCS

1. Зайдите в **WHMCS Admin Panel** (`https://bill.sthost.pro/admin`)
2. Перейдите в **Setup → Products/Services → Products/Services**
3. Создайте группу продуктов (если еще нет):
   - Нажмите **Create a New Group**
   - Название: `VPS Servers` или `VPS/VDS Сервери`
   - URL: `vps-servers`
   - Сохраните

4. Создайте продукты для каждого VPS плана:
   - Нажмите **Create a New Product**
   - Выберите тип: **Other** (или ваш модуль Proxmox/VPS)
   - Заполните данные:

#### VPS Start
- **Product Name**: VPS Start
- **Product Group**: VPS Servers
- **Payment Type**: Recurring
- **Pricing**:
  - Monthly: 299 грн
  - Annually: 2990 грн (10% скидка)
- **Description**: 1 vCPU, 1 GB RAM, 20 GB SSD
- Сохраните и **запомните Product ID** (например: 15)

#### VPS Basic (Популярный)
- **Product Name**: VPS Basic
- Monthly: 499 грн
- Annually: 4990 грн
- Description: 2 vCPU, 2 GB RAM, 40 GB SSD
- Сохраните и запомните Product ID (например: 16)

#### VPS Pro
- **Product Name**: VPS Pro
- Monthly: 899 грн
- Annually: 8990 грн
- Description: 4 vCPU, 4 GB RAM, 80 GB SSD
- Сохраните и запомните Product ID (например: 17)

#### VPS Business
- **Product Name**: VPS Business
- Monthly: 1599 грн
- Annually: 15990 грн
- Description: 6 vCPU, 8 GB RAM, 160 GB SSD
- Сохраните и запомните Product ID (например: 18)

---

### Шаг 2: Настройте маппинг Product IDs

Откройте файл: `/pages/vds/virtual.php`

Найдите в начале файла секцию **WHMCS INTEGRATION CONFIGURATION** (строки 5-46):

```php
$whmcs_config = [
    'billing_url' => 'https://bill.sthost.pro', // Ваш WHMCS URL

    // Маппінг VPS планів на Product IDs з WHMCS
    'product_ids' => [
        1 => 1,  // VPS Start -> WHMCS Product ID (ЗМІНІТЬ!)
        2 => 2,  // VPS Basic -> WHMCS Product ID (ЗМІНІТЬ!)
        3 => 3,  // VPS Pro -> WHMCS Product ID (ЗМІНІТЬ!)
        4 => 4   // VPS Business -> WHMCS Product ID (ЗМІНІТЬ!)
    ],
```

**Замените числа справа** на Product IDs из вашего WHMCS:

```php
'product_ids' => [
    1 => 15,  // VPS Start -> WHMCS Product ID 15
    2 => 16,  // VPS Basic -> WHMCS Product ID 16
    3 => 17,  // VPS Pro -> WHMCS Product ID 17
    4 => 18   // VPS Business -> WHMCS Product ID 18
],
```

---

### Шаг 3: Дополнительные настройки

#### Изменить биллинг цикл по умолчанию:
```php
'default_billing_cycle' => 'monthly', // monthly, quarterly, semiannually, annually
```

#### Включить прямой переход к оформлению заказа:
```php
'direct_checkout' => true, // true = сразу checkout, false = сначала cart
```

---

## ➕ Как добавить новый VPS план

### 1. Создайте продукт в WHMCS (как в Шаге 1 выше)

### 2. Добавьте план в файл `/pages/vds/virtual.php`

Найдите массив `$vps_plans` (примерно строка 91):

```php
$vps_plans = [
    // Существующие планы...

    // НОВЫЙ ПЛАН:
    [
        'id' => 5,                          // Уникальный ID (следующий по порядку)
        'name' => 'VPS Enterprise',         // Название плана
        'subtitle' => 'Для корпоративных проектів', // Подзаголовок
        'cpu_cores' => 8,                   // Количество vCPU
        'ram' => 16,                        // RAM в GB
        'storage' => 320,                   // Диск в GB
        'bandwidth' => 16000,               // Трафик в GB
        'price_monthly' => 2999,            // Цена в месяц (грн)
        'price_yearly' => 29990,            // Цена в год (грн)
        'is_popular' => 0,                  // 1 = показать бейдж "Найпопулярніший"
        'ipv4_addresses' => 2               // Количество IP адресов
    ]
];
```

### 3. Добавьте маппинг в WHMCS Product ID

В том же файле, в секции `$whmcs_config`:

```php
'product_ids' => [
    1 => 15,
    2 => 16,
    3 => 17,
    4 => 18,
    5 => 19   // <-- Добавьте новый план с WHMCS Product ID
],
```

### 4. Сохраните файл ✅

---

## 🔍 Проверка работы

1. Откройте страницу: `https://sthost.pro/pages/vds/virtual.php`
2. Нажмите кнопку **"Замовити зараз"** на любом плане
3. Вы будете перенаправлены на WHMCS: `https://bill.sthost.pro/cart.php?a=add&pid=XX`
4. Продукт должен быть добавлен в корзину ✅

---

## 📋 Формат ссылок WHMCS

Система генерирует ссылки в формате:

### Обычный режим (cart):
```
https://bill.sthost.pro/cart.php?a=add&pid=15
```
- `a=add` - добавить в корзину
- `pid=15` - Product ID

### Режим direct checkout:
```
https://bill.sthost.pro/cart.php?a=add&pid=15&billingcycle=monthly&carttpl=standard
```
- `billingcycle=monthly` - выбранный цикл оплаты
- `carttpl=standard` - шаблон корзины

---

## 🎨 Настройка отображения

### Изменить порядок планов:
Просто переставьте элементы массива `$vps_plans` местами.

### Скрыть план (временно):
Закомментируйте нужный план:
```php
/*
[
    'id' => 4,
    'name' => 'VPS Business',
    // ...
],
*/
```

### Изменить цены:
Измените значения `price_monthly` и `price_yearly` в массиве плана.

⚠️ **ВАЖНО**: Цены в WHMCS и в коде должны совпадать!

---

## 🛠️ Расширенная настройка

### Использование разных billing cycles для разных планов:

Измените секцию генерации URL (строка ~385):

```php
// Для разных планов разные циклы
$billing_cycles = [
    1 => 'monthly',
    2 => 'monthly',
    3 => 'quarterly',
    4 => 'annually'
];
$billing_cycle = $billing_cycles[$plan['id']] ?? 'monthly';
```

### Добавление промокодов:

```php
$order_url = "{$billing_url}/cart.php?a=add&pid={$whmcs_pid}&promocode=WELCOME10";
```

### Добавление доменов или аддонов:

```php
$order_url = "{$billing_url}/cart.php?a=add&pid={$whmcs_pid}&domain=example.com";
```

---

## ❗ Troubleshooting

### Кнопка не работает / открывается пустая страница:
1. Проверьте что Product ID существует в WHMCS
2. Проверьте что продукт **активен** (не hidden)
3. Проверьте консоль браузера на ошибки

### Неправильная цена в корзине:
1. Обновите цены в WHMCS Admin
2. Убедитесь что указан правильный billing cycle
3. Проверьте настройки валюты в WHMCS

### Product ID не найден:
Посмотрите Product ID в WHMCS:
- Setup → Products/Services → нажмите на продукт
- В URL будет: `...configproducts.php?action=edit&id=15` ← это ваш Product ID

---

## 📞 Поддержка

Если возникли вопросы по настройке:
1. Проверьте WHMCS логи: Setup → System Logs
2. Проверьте Activity Log в WHMCS
3. Используйте WHMCS Debug Mode для диагностики

---

## ✅ Готово!

После настройки все кнопки "Замовити зараз" будут автоматически вести на правильные страницы WHMCS для оформления заказа VPS.

**Файл конфигурации**: `/pages/vds/virtual.php` (строки 5-46)
**Дата создания**: 14.11.2024
