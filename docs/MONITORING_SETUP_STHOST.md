# 📊 Настройка мониторинга для StormHosting UA

**Специальная инструкция для вашей инфраструктуры**

## 🏗️ Инфраструктура StormHosting

```
┌─────────────────────────────────────────────────────────┐
│  Внешние каналы                                          │
│  • ns1.sthost.pro: 195.22.131.11                        │
│  • ns2.sthost.pro: 46.232.232.38                        │
└─────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│  HAProxy Load Balancer                                   │
│  IP: 192.168.0.10 (SSH port 22)                         │
└─────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┴────────────────┐
            ▼                                ▼
┌──────────────────────┐         ┌──────────────────────┐
│  Proxmox VE          │         │  ISPmanager          │
│  IP: 192.168.0.4     │         │  IP: 192.168.0.250   │
│  SSH: 225            │         │  SSH: 224            │
└──────────────────────┘         └──────────────────────┘
```

---

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Копируйте конфиг

```bash
cd /home/user/sthost_3.0
cp config/monitoring.config.sthost.php config/monitoring.config.php
```

### Шаг 2: Получите учетные данные

Следуйте инструкциям ниже для каждой системы ⬇️

### Шаг 3: Запустите

Откройте в браузере: `https://sthost.pro/server-status`

---

## 🔐 Получение учетных данных

## 1️⃣ ISPmanager (192.168.0.250)

### Вариант A: Использовать существующего админа

**Подключитесь к серверу:**
```bash
ssh -p 224 root@192.168.0.250
```

**Получите пароль root (если забыли):**
```bash
# Сбросить пароль ISPmanager admin
/usr/local/mgr5/sbin/mgrctl -m ispmgr user.passwd.set username=admin password=НОВЫЙ_ПАРОЛЬ
```

**В конфиге укажите:**
```php
'username' => 'admin',
'password' => 'ваш_пароль_admin',
```

### Вариант B: Создать отдельного пользователя для мониторинга (рекомендуется)

**1. Войдите в ISPmanager веб-интерфейс:**
```
https://192.168.0.250:1500
```

**2. Создайте пользователя:**
- Перейдите: **Пользователи** → **Создать**
- Логин: `monitoring`
- Пароль: `ваш_безопасный_пароль`
- Права: **Только чтение** (для безопасности)

**3. В конфиге укажите:**
```php
'username' => 'monitoring',
'password' => 'ваш_безопасный_пароль',
```

### Тестирование ISPmanager API:

```bash
# С вашего основного сервера проверьте доступ:
curl -k -u admin:PASSWORD https://192.168.0.250:1500/ispmgr?out=xml&func=stat
```

Должен вернуть XML с данными сервера.

---

## 2️⃣ Proxmox VE (192.168.0.4)

### Вариант A: Использовать root (простой способ)

**Подключитесь к серверу:**
```bash
ssh -p 225 root@192.168.0.4
```

**Узнайте имя ноды:**
```bash
hostname
# Вывод: pve (или другое имя)
```

**В конфиге укажите:**
```php
'node' => 'pve',              // Замените на вывод команды hostname
'username' => 'root@pam',
'password' => 'ваш_root_пароль',
```

### Вариант B: Создать API токен (безопаснее) ⭐ РЕКОМЕНДУЕТСЯ

**1. Подключитесь к Proxmox:**
```bash
ssh -p 225 root@192.168.0.4
```

**2. Создайте пользователя для мониторинга:**
```bash
pveum user add monitoring@pve --comment "Monitoring User"
pveum aclmod / -user monitoring@pve -role PVEAuditor
```

**3. Создайте API токен:**
```bash
pveum user token add monitoring@pve monitoring-token --privsep 0
```

**ВАЖНО:** Сохраните токен! Он показывается один раз:
```
┌──────────────┬──────────────────────────────────────┐
│ key          │ value                                │
│ full-tokenid │ monitoring@pve!monitoring-token      │
│ value        │ xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx │
└──────────────┴──────────────────────────────────────┘
```

**4. В конфиге укажите:**
```php
'username' => 'monitoring@pve',
'password' => 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx',  // Ваш токен
```

### Тестирование Proxmox API:

```bash
# Проверьте доступ:
curl -k -u root@pam:PASSWORD https://192.168.0.4:8006/api2/json/version

# Проверьте список нод:
curl -k -u root@pam:PASSWORD https://192.168.0.4:8006/api2/json/nodes
```

---

## 3️⃣ HAProxy (192.168.0.10)

### Включить и настроить Stats

**1. Подключитесь к серверу HAProxy:**
```bash
ssh root@192.168.0.10
```

**2. Отредактируйте конфигурацию:**
```bash
nano /etc/haproxy/haproxy.cfg
```

**3. Добавьте в конец файла:**
```haproxy
# Stats для мониторинга
listen stats
    bind *:8080
    mode http
    stats enable
    stats uri /stats
    stats refresh 30s
    stats realm HAProxy\ Statistics
    stats auth admin:ваш_безопасный_пароль
    stats admin if TRUE
```

**4. Проверьте конфигурацию:**
```bash
haproxy -c -f /etc/haproxy/haproxy.cfg
```

Должно быть: `Configuration file is valid`

**5. Перезапустите HAProxy:**
```bash
systemctl restart haproxy
systemctl status haproxy
```

**6. Проверьте что порт открыт:**
```bash
netstat -tlnp | grep 8080
```

**7. В конфиге мониторинга укажите:**
```php
'stats_url' => 'http://192.168.0.10:8080/stats',
'stats_user' => 'admin',
'stats_password' => 'ваш_безопасный_пароль',
```

### Тестирование HAProxy Stats:

**В браузере откройте:**
```
http://192.168.0.10:8080/stats
```

Введите логин/пароль. Должна открыться страница со статистикой.

**Или через curl:**
```bash
curl -u admin:PASSWORD http://192.168.0.10:8080/stats
```

---

## 4️⃣ SNMP для мониторинга сетевых каналов

### На сервере HAProxy (192.168.0.10) - точка мониторинга каналов

**1. Подключитесь к HAProxy:**
```bash
ssh root@192.168.0.10
```

**2. Установите SNMP:**
```bash
apt-get update
apt-get install snmpd snmp
```

**3. Настройте SNMP:**
```bash
nano /etc/snmp/snmpd.conf
```

**Найдите и раскомментируйте/добавьте:**
```conf
# Разрешить доступ с localhost
rocommunity public localhost
rocommunity public 192.168.0.0/24

# Слушать на всех интерфейсах
agentAddress udp:161

# Информация о системе
sysLocation    StormHosting UA, Dnipro
sysContact     support@sthost.pro
```

**4. Перезапустите SNMP:**
```bash
systemctl restart snmpd
systemctl enable snmpd
systemctl status snmpd
```

**5. Узнайте имена интерфейсов:**
```bash
snmpwalk -v2c -c public localhost IF-MIB::ifDescr
```

Вывод будет примерно таким:
```
IF-MIB::ifDescr.1 = STRING: lo
IF-MIB::ifDescr.2 = STRING: eth0      ← Первый канал
IF-MIB::ifDescr.3 = STRING: eth1      ← Второй канал
```

**6. Проверьте какой интерфейс какой канал:**
```bash
ip addr show

# Или
ip route | grep default
```

**7. В конфиге укажите правильные интерфейсы:**
```php
// Для канала ns1.sthost.pro (195.22.131.11)
[
    'interface' => 'eth0',  // Замените на ваш интерфейс
],

// Для канала ns2.sthost.pro (46.232.232.38)
[
    'interface' => 'eth1',  // Замените на ваш интерфейс
],
```

### Тестирование SNMP:

```bash
# Проверка доступности SNMP:
snmpget -v2c -c public 192.168.0.10 SNMPv2-MIB::sysDescr.0

# Проверка трафика на интерфейсе (замените .2 на ваш):
snmpget -v2c -c public 192.168.0.10 IF-MIB::ifInOctets.2
snmpget -v2c -c public 192.168.0.10 IF-MIB::ifOutOctets.2
```

---

## 🔧 Финальная настройка конфига

Откройте конфиг:
```bash
nano /home/user/sthost_3.0/config/monitoring.config.php
```

### Замените все ⚠️ маркеры:

```php
// ISPmanager
'username' => 'admin',              // Ваш логин
'password' => 'ваш_пароль',        // ⚠️ ЗАПОЛНИТЕ

// Proxmox
'node' => 'pve',                    // ⚠️ ПРОВЕРЬТЕ командой: hostname
'password' => 'ваш_пароль',        // ⚠️ ЗАПОЛНИТЕ

// HAProxy
'stats_password' => 'ваш_пароль',  // ⚠️ ЗАПОЛНИТЕ

// SNMP
'community' => 'public',            // ⚠️ ЗАПОЛНИТЕ (из snmpd.conf)
'interface' => 'eth0',              // ⚠️ ПРОВЕРЬТЕ (snmpwalk)
```

---

## ✅ Проверка работоспособности

### 1. Проверка конфигурации PHP

```bash
# Проверьте что PHP может прочитать конфиг:
php -r "print_r(require('/home/user/sthost_3.0/config/monitoring.config.php'));"
```

Должен вывести массив с настройками.

### 2. Тест API endpoint

```bash
# Проверьте API мониторинга:
curl http://localhost/api/monitoring/status.php?action=all

# Или с форматом simple:
curl http://localhost/api/monitoring/status.php?action=all&format=simple
```

Должен вернуть JSON с данными серверов.

### 3. Проверка страницы

Откройте в браузере:
```
http://sthost.pro/server-status
```

Вы должны увидеть:
- ✅ Статус всех серверов
- ✅ Графики загрузки CPU/RAM
- ✅ Данные по сетевым каналам
- ✅ Статус HAProxy backends

---

## 🎯 Настройка Telegram уведомлений (опционально)

### 1. Создайте Telegram бота

**В Telegram найдите @BotFather и отправьте:**
```
/newbot
```

**Следуйте инструкциям:**
- Введите имя: `StormHosting Monitor`
- Введите username: `stormhosting_monitor_bot`

**Сохраните токен:**
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

### 2. Получите Chat ID

**Напишите боту любое сообщение, затем выполните:**
```bash
curl https://api.telegram.org/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789/getUpdates
```

**Найдите в ответе:**
```json
"chat": {
  "id": 123456789,
  ...
}
```

### 3. В конфиге укажите:

```php
'telegram' => [
    'enabled' => true,
    'bot_token' => '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789',
    'chat_id' => '123456789',
],
```

---

## 🐛 Типичные проблемы и решения

### Проблема: "Failed to connect to 192.168.0.250"

**Решение:**
```bash
# Проверьте доступность сервера:
ping 192.168.0.250

# Проверьте порт:
telnet 192.168.0.250 1500

# Проверьте файрвол на ISPmanager:
ssh -p 224 root@192.168.0.250
iptables -L | grep 1500
```

### Проблема: "SNMP extension not installed"

**Решение:**
```bash
# На основном сервере:
apt-get install php-snmp
systemctl restart apache2

# Проверка:
php -m | grep snmp
```

### Проблема: "Authentication failed" для Proxmox

**Решение:**
```bash
# На Proxmox сервере проверьте пользователя:
ssh -p 225 root@192.168.0.4
pveum user list

# Проверьте права:
pveum user permissions monitoring@pve
```

### Проблема: Медленная загрузка страницы

**Решение - увеличьте кеш:**
```php
'general' => [
    'cache_ttl' => 120,  // 2 минуты вместо 60 секунд
],
```

---

## 📊 Что мониторится

### ISPmanager (192.168.0.250)
- ✅ CPU загрузка (%)
- ✅ RAM использование (%)
- ✅ Disk использование (%)
- ✅ Network трафик
- ✅ Uptime сервера

### Proxmox VE (192.168.0.4)
- ✅ CPU загрузка ноды (%)
- ✅ RAM использование (%)
- ✅ Disk использование (%)
- ✅ Список всех VM
- ✅ Статус каждой VM
- ✅ Ресурсы каждой VM

### HAProxy (192.168.0.10)
- ✅ Статус всех frontend
- ✅ Статус всех backend
- ✅ Список серверов в пулах
- ✅ Количество активных сессий
- ✅ Статистика по запросам

### Сетевые каналы
- ✅ ns1.sthost.pro (195.22.131.11) - RX/TX
- ✅ ns2.sthost.pro (46.232.232.38) - RX/TX
- ✅ Процент использования канала
- ✅ Текущая скорость (Mbps)

---

## 🆘 Поддержка

**Если что-то не работает:**

1. Проверьте логи PHP:
```bash
tail -f /var/log/apache2/error.log
```

2. Проверьте логи систем:
```bash
# ISPmanager
ssh -p 224 root@192.168.0.250 'tail -f /usr/local/mgr5/var/ispmgr.log'

# Proxmox
ssh -p 225 root@192.168.0.4 'tail -f /var/log/pve/tasks/active'

# HAProxy
ssh root@192.168.0.10 'tail -f /var/log/haproxy.log'
```

3. Очистите кеш:
```bash
rm /tmp/ispmanager_*.cache
rm /tmp/proxmox_*.cache
rm /tmp/haproxy_*.cache
rm /tmp/network_*.cache
```

4. Свяжитесь с поддержкой: support@sthost.pro

---

**Версия:** 1.0.0
**Дата:** 19.11.2024
**Автор:** StormHosting UA IT Team
