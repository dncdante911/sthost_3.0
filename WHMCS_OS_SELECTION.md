# 📘 Настройка выбора операционных систем для VPS

## 🎯 Как это работает

Пользователи могут:
1. **Выбрать ОС** на странице перед заказом (кликабельные карточки)
2. **Видят индикатор** выбранной ОС над тарифами
3. **Автоматический скролл** к тарифам после выбора
4. Выбор ОС **передается в WHMCS** через URL параметры

---

## ⚙️ Настройка в WHMCS (Configurable Options)

### Шаг 1: Создайте Configurable Option Group

1. Зайдите в **WHMCS Admin** → **Setup → Products/Services → Configurable Options**
2. Нажмите **Create a New Group**
3. Заполните:
   - **Group Name**: `VPS Operating Systems`
   - **Group Description**: `Choose your operating system`
4. Сохраните, запомните **Group ID** (например: 1)

---

### Шаг 2: Создайте Configurable Option для ОС

1. Откройте созданную группу
2. Нажмите **Add New Configurable Option**
3. Заполните:

**Basic Settings:**
- **Option Name**: `Operating System` или `Операційна система`
- **Option Type**: **Dropdown** (выпадающий список)
- **Description**: `Select your preferred OS`

**Option Values** (добавьте все ОС):

| Option Name | Option Value | Monthly Price | Setup Fee |
|-------------|--------------|---------------|-----------|
| Ubuntu 22.04 LTS | ubuntu-22 | 0.00 | 0.00 |
| CentOS Stream 8 | centos-8 | 0.00 | 0.00 |
| Debian 11 | debian-11 | 0.00 | 0.00 |
| AlmaLinux 8 | almalinux-8 | 0.00 | 0.00 |
| Rocky Linux 8 | rocky-8 | 0.00 | 0.00 |
| FreeBSD 13 | freebsd-13 | 0.00 | 0.00 |
| Windows Server 2022 | windows-2022 | 500.00 | 0.00 |
| Windows Server 2019 | windows-2019 | 400.00 | 0.00 |

⚠️ **ВАЖНО**: `Option Value` должны совпадать с `id` в массиве `$operating_systems` в файле `virtual.php`

4. **Сохраните**

---

### Шаг 3: Привяжите к продуктам VPS

1. Перейдите в **Setup → Products/Services → Products/Services**
2. Откройте каждый VPS продукт (VPS Start, Basic, Pro, Business)
3. Вкладка **Configurable Options**
4. Отметьте галочкой **VPS Operating Systems**
5. Сохраните для каждого продукта

---

### Шаг 4: Настройте передачу параметра

В файле `/pages/vds/virtual.php` уже настроена передача через URL:

```javascript
baseUrl += `&configoption[1]=${encodeURIComponent(selectedOS.id)}`;
```

Где `[1]` - это **ID вашего Configurable Option Group**.

**Как узнать ID:**
- В WHMCS Admin → Configurable Options
- Наведите на группу, в URL будет: `...&id=1` ← это ваш ID

**Если ваш ID другой** (например 5), измените в файле `virtual.php` (строка 868):
```javascript
baseUrl += `&configoption[5]=${encodeURIComponent(selectedOS.id)}`;
```

---

## 🎨 Как выглядит для пользователя

### 1. Выбор ОС
- Пользователь кликает на карточку ОС
- Карточка подсвечивается с галочкой ✓
- Появляется зеленый индикатор "Обрана ОС: Ubuntu 22.04 LTS"
- Автоматический скролл к тарифам

### 2. Заказ VPS
- Нажимает "Замовити зараз"
- Переходит в WHMCS
- В корзине ОС уже **предвыбрана** ✅

---

## 💡 Альтернативный подход (без Configurable Options)

Если вы **НЕ хотите использовать Configurable Options**, можно:

### Вариант 1: Создать отдельные продукты для каждой ОС
❌ **Минусы**:
- 4 плана × 8 ОС = 32 продукта
- Сложно управлять
- Путаница для клиентов

### Вариант 2: Убрать выбор ОС со страницы
- Удалить секцию "Операційні системи"
- Клиенты выбирают ОС после заказа в панели управления VPS

### Вариант 3: Оставить только информацию (текущая реализация БЕЗ передачи в WHMCS)
- Удалить строку 868 в virtual.php:
```javascript
// baseUrl += `&configoption[1]=${encodeURIComponent(selectedOS.id)}`;
```
- ОС будут показываться просто для информации

---

## 🔧 Кастомизация

### Добавить новую ОС

1. **В файле `/pages/vds/virtual.php`** (строка ~147):
```php
$operating_systems = [
    // Существующие ОС...
    ['id' => 'ubuntu-24', 'name' => 'Ubuntu 24.04 LTS', 'icon' => 'ubuntu.png', 'category' => 'Linux', 'popular' => true],
];
```

2. **В WHMCS Configurable Options**:
- Добавьте новый Option Value с таким же `id` ('ubuntu-24')

### Изменить цены на Windows

В WHMCS Configurable Options → Edit Option → Windows Server 2022:
- Monthly Price: 500.00 грн (или ваша цена)

### Сделать ОС обязательной для выбора

Добавьте проверку перед переходом на заказ (в virtual.php, строка ~796):

```javascript
function selectOS(osId, osName) {
    // существующий код...
}

// Перед переходом проверяем
document.querySelectorAll('.btn-order-vps').forEach(btn => {
    btn.addEventListener('click', function(e) {
        if (!selectedOS) {
            e.preventDefault();
            alert('⚠️ Будь ласка, оберіть операційну систему!');
            document.getElementById('operating-systems')?.scrollIntoView({ behavior: 'smooth' });
        }
    });
});
```

---

## 📊 Формат ссылок

### Без выбора ОС:
```
https://bill.sthost.pro/cart.php?a=add&pid=15&billingcycle=monthly
```

### С выбранной ОС:
```
https://bill.sthost.pro/cart.php?a=add&pid=15&billingcycle=monthly&configoption[1]=ubuntu-22
```

---

## ✅ Проверка работы

1. Откройте `https://sthost.pro/pages/vds/virtual.php`
2. Кликните на любую ОС → должна подсветиться с галочкой
3. Появится зеленый индикатор "Обрана ОС"
4. Нажмите "Замовити зараз"
5. В WHMCS корзине проверьте что ОС предвыбрана

---

## 🎯 Мое рекомендованное решение

**Используйте Configurable Options** потому что:
- ✅ Централизованное управление
- ✅ Гибкость в ценах (Windows дороже)
- ✅ Легко добавлять новые ОС
- ✅ Клиенты видят выбор ДО заказа
- ✅ Автоматическая передача в WHMCS
- ✅ Стандартный подход в индустрии

---

**Дата создания**: 14.11.2024
**Файлы**:
- `/pages/vds/virtual.php` - главный файл с выбором ОС
- `WHMCS_VPS_INTEGRATION.md` - общая интеграция с WHMCS
