# 🧹 Файлы для удаления (Cleanup List)

**Дата создания**: 2025-11-19
**Цель**: Список дубликатов, неиспользуемых и устаревших файлов

---

## ⚠️ ПЕРЕД УДАЛЕНИЕМ

**ВАЖНО**: Перед удалением файлов:
1. Создайте резервную копию: `tar -czf backup-$(date +%Y%m%d).tar.gz /var/www/sthost_3.0`
2. Проверьте что файлы действительно не используются
3. Удаляйте по одному файлу и тестируйте сайт
4. Commit изменения в git после каждого шага

---

## 📂 ДУБЛИКАТЫ СТРАНИЦ

### Dashboard (Панель управления клиента)

**Проблема**: 3 версии dashboard - нужна только одна!

| Файл | Статус | Действие |
|------|--------|----------|
| `/client/dashboard-new.php` | ✅ **ИСПОЛЬЗУЕТСЯ** | **ОСТАВИТЬ** (это актуальная версия) |
| `/client/dashboard.php` | ⚠️ СТАРАЯ ВЕРСИЯ | **УДАЛИТЬ** после проверки |
| `/client/dashboard_debug.php` | ❌ DEBUG версия | **УДАЛИТЬ** |

**CSS файлы:**
| Файл | Статус | Действие |
|------|--------|----------|
| `/assets/css/dashboard-new.css` | ✅ **ИСПОЛЬЗУЕТСЯ** | **ОСТАВИТЬ** |
| `/assets/css/dashboard.css` | ⚠️ СТАРАЯ ВЕРСИЯ | **УДАЛИТЬ** после проверки |

**JS файлы:**
| Файл | Статус | Действие |
|------|--------|----------|
| `/assets/js/dashboard-new.js` | ✅ **ИСПОЛЬЗУЕТСЯ** | **ОСТАВИТЬ** |
| `/assets/js/dashboard.js` | ⚠️ СТАРАЯ ВЕРСИЯ | **УДАЛИТЬ** после проверки |

**Команда для удаления**:
```bash
# Проверьте что dashboard-new.php работает!
rm /var/www/sthost_3.0/client/dashboard.php
rm /var/www/sthost_3.0/client/dashboard_debug.php
rm /var/www/sthost_3.0/assets/css/dashboard.css
rm /var/www/sthost_3.0/assets/js/dashboard.js
```

---

### Калькуляторы хостинга

**Проблема**: 2 версии калькулятора

| Файл | Статус | Действие |
|------|--------|----------|
| `/pages/hosting/calculator.php` | ✅ **ИСПОЛЬЗУЕТСЯ** | **ОСТАВИТЬ** |
| `/pages/hosting/calculator1.php` | ❌ ДУБЛИКАТ | **УДАЛИТЬ** |

**Команда для удаления**:
```bash
rm /var/www/sthost_3.0/pages/hosting/calculator1.php
```

---

## 🧪 ТЕСТОВЫЕ ФАЙЛЫ

| Файл | Назначение | Действие |
|------|-----------|----------|
| `/pages/tools/test-ip-check-api.php` | Тестирование IP Check API | **УДАЛИТЬ** (или переместить в `/test/`) |

**Команда для удаления**:
```bash
rm /var/www/sthost_3.0/pages/tools/test-ip-check-api.php
```

---

## 🎨 НЕИСПОЛЬЗУЕМЫЕ CSS ФАЙЛЫ

### Возможно неиспользуемые (требуется проверка):

| Файл | Страница | Проверка |
|------|---------|----------|
| `/assets/css/_variables.css` | Общие переменные | Проверьте импорт в main.css |
| `/assets/css/utilities.css` | Утилиты | Проверьте импорт в main.css |
| `/assets/css/premium-enhancements.css` | Премиум улучшения | Найдите где подключается |
| `/assets/css/vps-management.css` | VPS управление | `/client/vps.php` использует inline стили! |

**Проверка используется ли CSS**:
```bash
# Поиск где подключается файл
grep -r "premium-enhancements.css" /var/www/sthost_3.0/
grep -r "vps-management.css" /var/www/sthost_3.0/
grep -r "_variables.css" /var/www/sthost_3.0/
```

**Если файл НЕ найден - удаляйте**:
```bash
# ТОЛЬКО если grep выше ничего не нашел!
rm /var/www/sthost_3.0/assets/css/vps-management.css
rm /var/www/sthost_3.0/assets/css/premium-enhancements.css
```

---

## 📜 НЕИСПОЛЬЗУЕМЫЕ JS ФАЙЛЫ

### Возможно неиспользуемые:

| Файл | Страница | Проверка |
|------|---------|----------|
| `/assets/js/vps-management.js` | VPS управление | `/client/vps.php` использует inline JS! |
| `/assets/js/logger.js` | Логгер | Проверьте импорт |
| `/assets/js/security-protection.js` | Защита безопасности | Проверьте импорт |

**Проверка используется ли JS**:
```bash
grep -r "vps-management.js" /var/www/sthost_3.0/
grep -r "logger.js" /var/www/sthost_3.0/
grep -r "security-protection.js" /var/www/sthost_3.0/
```

---

## 📚 УСТАРЕВШИЕ БИБЛИОТЕКИ

### Vue.js (не используется?)

| Файл | Размер | Действие |
|------|--------|----------|
| `/assets/js/libs/vue.global.js` | ~500KB | Проверьте используется ли Vue на сайте |

**Проверка**:
```bash
grep -r "vue.global.js" /var/www/sthost_3.0/
grep -r "Vue.createApp" /var/www/sthost_3.0/
```

**Если НЕ используется - удаляйте**:
```bash
rm /var/www/sthost_3.0/assets/js/libs/vue.global.js
```

---

## 🗂️ УСТАРЕВШИЕ INCLUDES

### FOSSBilling (DEPRECATED!)

**Проблема**: Старый код FOSSBilling, сейчас используется WHMCS или новый FossBilling через API

| Файл | Статус | Действие |
|------|--------|----------|
| `/includes/fossbilling_client.php` | ❌ DEPRECATED | **УДАЛИТЬ** если используете WHMCS |

**В config.php** (строки 30-33) уже есть комментарий:
```php
// Старая конфигурация FOSSBilling (DEPRECATED - больше не используется)
```

**Команда для удаления**:
```bash
# Если используете WHMCS, а не старый FOSSBilling
rm /var/www/sthost_3.0/includes/fossbilling_client.php
```

---

## 📄 ДУБЛИКАТЫ СТРАНИЦ

### Страницы которые могут быть дубликатами:

| Файл | Альтернатива | Проверка |
|------|-------------|----------|
| `/pages/domains/domains.php` | Главная страница доменов? | Сравните с `/pages/home.php` |
| `/pages/hosting/hosting.php` | Главная страница хостинга? | Проверьте содержимое |

**Проверьте содержимое**:
```bash
wc -l /var/www/sthost_3.0/pages/domains/domains.php
wc -l /var/www/sthost_3.0/pages/hosting/hosting.php
```

---

## 🎨 CSS ФАЙЛЫ ДЛЯ АНАЛИЗА

### Многие CSS файлы в pages/ могут быть для конкретных страниц

Проверьте используются ли эти CSS файлы:

```bash
# Список всех CSS файлов
ls -lh /var/www/sthost_3.0/assets/css/pages/

# Проверьте каждый файл
for file in /var/www/sthost_3.0/assets/css/pages/*.css; do
    filename=$(basename "$file")
    count=$(grep -r "$filename" /var/www/sthost_3.0/ --exclude-dir=assets | wc -l)
    if [ $count -eq 0 ]; then
        echo "❌ НЕ ИСПОЛЬЗУЕТСЯ: $filename"
    else
        echo "✅ ИСПОЛЬЗУЕТСЯ ($count раз): $filename"
    fi
done
```

**Возможные кандидаты на удаление** (проверьте скриптом выше):
- `/assets/css/pages/domains2.css` (если есть `/assets/css/pages/domains.css`)
- `/assets/css/pages/domains-register-2.css` (если есть другая версия)
- `/assets/css/pages/tools-ip-check2.css` (если есть `tools-ip-check.css`)
- `/assets/css/pages/client-profile.css` (дубликат с `/assets/css/client-profile.css`)

---

## 🗄️ НЕИСПОЛЬЗУЕМЫЕ MD ФАЙЛЫ (документация)

### Документация которая может быть устаревшей:

| Файл | Актуальность | Действие |
|------|-------------|----------|
| `ADMIN_README.md` | Проверьте содержимое | Объедините с README_RU.md или удалите |
| `PROXMOX_SETUP_GUIDE.md` | Проверьте содержимое | Объедините с README_RU.md или удалите |
| `WHMCS_OS_SELECTION.md` | Проверьте содержимое | Объедините с README_RU.md или удалите |
| `SECURITY_AUDIT_REPORT.md` | Отчет аудита | Заархивируйте или удалите |
| `migrations/README.md` | Описание миграций | Оставьте или объедините |

**Рекомендация**: Объедините всю документацию в `README_RU.md` (уже создан!)

```bash
# Заархивируйте старые README перед удалением
tar -czf old-docs-$(date +%Y%m%d).tar.gz \
    ADMIN_README.md \
    PROXMOX_SETUP_GUIDE.md \
    WHMCS_OS_SELECTION.md \
    SECURITY_AUDIT_REPORT.md

# Затем удалите (ОПЦИОНАЛЬНО!)
# rm ADMIN_README.md PROXMOX_SETUP_GUIDE.md WHMCS_OS_SELECTION.md SECURITY_AUDIT_REPORT.md
```

---

## 📊 СТАТИСТИКА ПРОЕКТА

### Текущее состояние:

```bash
# Общее количество PHP файлов
find /var/www/sthost_3.0 -name "*.php" | wc -l

# Общее количество CSS файлов
find /var/www/sthost_3.0 -name "*.css" | wc -l

# Общее количество JS файлов
find /var/www/sthost_3.0 -name "*.js" | wc -l

# Размер проекта
du -sh /var/www/sthost_3.0

# Размер assets
du -sh /var/www/sthost_3.0/assets
```

---

## ✅ БЕЗОПАСНОЕ УДАЛЕНИЕ (Пошаговый план)

### Шаг 1: Создайте резервную копию

```bash
cd /var/www
tar -czf sthost_backup_$(date +%Y%m%d).tar.gz sthost_3.0/
```

### Шаг 2: Удалите ТОЧНО ненужные файлы

```bash
cd /var/www/sthost_3.0

# 1. Debug файлы
rm client/dashboard_debug.php

# 2. Тестовые файлы
rm pages/tools/test-ip-check-api.php

# 3. Дубликаты
rm pages/hosting/calculator1.php

# 4. Устаревший FOSSBilling (ТОЛЬКО если используете WHMCS!)
# rm includes/fossbilling_client.php
```

### Шаг 3: Проверьте сайт работает

```bash
# Проверьте все основные страницы:
curl -I https://sthost.pro
curl -I https://sthost.pro/client/vps.php
curl -I https://sthost.pro/pages/domains/whois.php
```

### Шаг 4: Commit изменения

```bash
git add -A
git commit -m "🧹 Cleanup: Remove duplicate and unused files"
git push
```

### Шаг 5: Удалите старые dashboard файлы (ОПЦИОНАЛЬНО)

**ТОЛЬКО если dashboard-new.php работает идеально!**

```bash
# Проверьте dashboard-new.php работает
curl -I https://sthost.pro/client/dashboard-new.php

# Если OK - удалите старые
rm /var/www/sthost_3.0/client/dashboard.php
rm /var/www/sthost_3.0/assets/css/dashboard.css
rm /var/www/sthost_3.0/assets/js/dashboard.js
```

---

## 🔍 СКРИПТ ДЛЯ ПОИСКА НЕИСПОЛЬЗУЕМЫХ ФАЙЛОВ

### Автоматический поиск неиспользуемых CSS/JS

Создайте скрипт `/tmp/find_unused.sh`:

```bash
#!/bin/bash

echo "🔍 Поиск неиспользуемых CSS файлов..."
echo "======================================"

for file in /var/www/sthost_3.0/assets/css/**/*.css; do
    if [ -f "$file" ]; then
        filename=$(basename "$file")
        # Поиск упоминаний файла (исключая саму папку assets)
        count=$(grep -r "$filename" /var/www/sthost_3.0/ \
                --exclude-dir=assets \
                --exclude-dir=.git \
                --exclude="*.md" | wc -l)

        if [ $count -eq 0 ]; then
            size=$(du -h "$file" | cut -f1)
            echo "❌ $filename ($size) - НЕ ИСПОЛЬЗУЕТСЯ"
        fi
    fi
done

echo ""
echo "🔍 Поиск неиспользуемых JS файлов..."
echo "====================================="

for file in /var/www/sthost_3.0/assets/js/**/*.js; do
    if [ -f "$file" ]; then
        filename=$(basename "$file")
        count=$(grep -r "$filename" /var/www/sthost_3.0/ \
                --exclude-dir=assets \
                --exclude-dir=.git \
                --exclude="*.md" | wc -l)

        if [ $count -eq 0 ]; then
            size=$(du -h "$file" | cut -f1)
            echo "❌ $filename ($size) - НЕ ИСПОЛЬЗУЕТСЯ"
        fi
    fi
done
```

Запустите:
```bash
chmod +x /tmp/find_unused.sh
/tmp/find_unused.sh
```

---

## 📋 ИТОГОВЫЙ СПИСОК ДЛЯ УДАЛЕНИЯ

### МОЖНО УДАЛЯТЬ БЕЗОПАСНО:

```bash
# Debug и тестовые файлы
/client/dashboard_debug.php
/pages/tools/test-ip-check-api.php
/pages/hosting/calculator1.php
```

### ПРОВЕРЬТЕ ПЕРЕД УДАЛЕНИЕМ:

```bash
# Старые версии (проверьте что новые работают!)
/client/dashboard.php
/assets/css/dashboard.css
/assets/js/dashboard.js

# Устаревшие интеграции
/includes/fossbilling_client.php  # ТОЛЬКО если используете WHMCS!

# Неиспользуемые стили/скрипты (проверьте grep!)
/assets/css/vps-management.css
/assets/js/vps-management.js
/assets/css/premium-enhancements.css
/assets/js/libs/vue.global.js
```

---

## 💡 РЕКОМЕНДАЦИИ

1. **НЕ удаляйте всё сразу** - делайте по одному файлу и тестируйте
2. **Всегда делайте backup** перед удалением
3. **Используйте git** - можно откатить изменения
4. **Проверяйте скриптом** `/tmp/find_unused.sh` перед удалением
5. **Тестируйте сайт** после каждого удаления

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После очистки должно освободиться:

- **~50-100MB** места на диске
- **3-5 дубликатов** страниц удалено
- **5-10 неиспользуемых** CSS/JS файлов удалено
- **Упрощение** структуры проекта

---

**Дата создания**: 2025-11-19
**Версия**: 1.0
